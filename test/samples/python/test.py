import os
from pprint import pprint

def foo(a, b):
    w = 4
    x = "hello"
    y = a
    z = [4, "hello", a]
    q = a * b + w
    q = (a * b) + w
    q = a * (b + w)
    r = (a + b) - w
    s = a * (b / w)
    t = a + print(1, a, "b", ["hello"])
    print("hello world!")
